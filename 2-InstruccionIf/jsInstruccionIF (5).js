function mostrar()
{
//tomo la edad  
var edadUser;

edadUser = edad.value;
edadUser = parseInt(edadUser);

if (edadUser > 17 || edadUser < 13)
 {
	alert("No es adolescente");
 }
}//FIN DE LA FUNCIÓN